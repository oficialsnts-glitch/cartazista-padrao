/* =====================================================================
   CARTAZISTA PRO — Runtime Config
   Edite APENAS este arquivo para apontar o frontend para o backend correto.
   ===================================================================== */
window.CARTAZISTA_CONFIG = {
  // URL base do backend (Render).
  // - Local:  "http://localhost:8001"
  // - Render: "https://SEU-BACKEND.onrender.com"
  // Deixe vazio ("") para usar o mesmo domínio do frontend (útil em preview Emergent).
  API_BASE: "",
};
